import React, { Component } from 'react'

export default class Galerie extends Component {
  render() {
    return (
      <div>
        Galeria component 
        'Dividir por categoría técnica ' (agregar un sort by character?)
      </div>
    )
  }
}
